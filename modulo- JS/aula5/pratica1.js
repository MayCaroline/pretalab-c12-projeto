let i = 1; // Contador

while (i <= 20) {
   if (i % 2 !== 0) { // Verifica se o número é ímpar se o resto for igual a 0 é par se for diferente é impar
       console.log(i);
   }
   i++; // Incrementa o contador
}